<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_volontariato_tipo_cert_volontario_list" model="ir.ui.view">
        <field name="name">volontariato.tipo.cert.volontario.list</field>
        <field name="model">volontariato.tipo.cert.volontario</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="action_volontariato_tipo_cert_volontario" model="ir.actions.act_window">
        <field name="name">Tipi Certificazione Volontari</field>
        <field name="res_model">volontariato.tipo.cert.volontario</field>
        <field name="view_mode">list</field>
    </record>

</odoo>
