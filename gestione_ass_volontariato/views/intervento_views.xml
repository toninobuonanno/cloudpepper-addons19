<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ═══════════ FORM VIEW ═══════════ -->
    <record id="view_volontariato_intervento_form" model="ir.ui.view">
        <field name="name">volontariato.intervento.form</field>
        <field name="model">volontariato.intervento</field>
        <field name="arch" type="xml">
            <form string="Foglio di Viaggio">
                <header>
                    <button name="action_confirm" type="object" string="Conferma"
                            class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_cancel" type="object" string="Annulla"
                            invisible="state == 'cancelled'"/>
                    <button name="action_set_draft" type="object" string="Imposta a Bozza"
                            invisible="state != 'cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="codice" readonly="1"/>
                        </h1>
                    </div>

                    <group invisible="state != 'draft'">
                        <group string="Sezione 1 - Dati Intervento">
                            <field name="vehicle_id"/>
                            <field name="tipo_intervento"/>
                            <field name="data"/>
                            <field name="ora_inizio" widget="float_time"/>
                            <field name="ora_fine" widget="float_time"/>
                            <field name="km_iniziali"/>
                            <field name="km_finali"/>
                            <field name="km_totali" readonly="1"/>
                        </group>
                        <group string="Orari sul Luogo">
                            <field name="ora_arrivo_luogo" widget="float_time"/>
                            <field name="ora_partenza_luogo" widget="float_time"/>
                            <field name="ora_arrivo_dest" widget="float_time"/>
                            <field name="durata_ore" widget="float_time" readonly="1"/>
                        </group>
                    </group>

                    <group invisible="state == 'draft'">
                        <group string="Sezione 1 - Dati Intervento">
                            <field name="vehicle_id" readonly="1"/>
                            <field name="tipo_intervento" readonly="1"/>
                            <field name="data" readonly="1"/>
                            <field name="ora_inizio" widget="float_time" readonly="1"/>
                            <field name="ora_fine" widget="float_time" readonly="1"/>
                            <field name="km_iniziali" readonly="1"/>
                            <field name="km_finali" readonly="1"/>
                            <field name="km_totali" readonly="1"/>
                        </group>
                        <group string="Orari sul Luogo">
                            <field name="ora_arrivo_luogo" widget="float_time" readonly="1"/>
                            <field name="ora_partenza_luogo" widget="float_time" readonly="1"/>
                            <field name="ora_arrivo_dest" widget="float_time" readonly="1"/>
                            <field name="durata_ore" widget="float_time" readonly="1"/>
                        </group>
                    </group>

                    <group>
                        <group string="Richiedente">
                            <field name="richiedente" readonly="state != 'draft'"/>
                            <field name="richiedente_citta" readonly="state != 'draft'"/>
                            <field name="richiedente_provincia" readonly="state != 'draft'"/>
                        </group>
                        <group string="Luogo">
                            <field name="luogo" readonly="state != 'draft'"/>
                            <field name="luogo_citta" readonly="state != 'draft'"/>
                            <field name="luogo_provincia" readonly="state != 'draft'"/>
                            <field name="luogo_destinazione" readonly="state != 'draft'"/>
                        </group>
                    </group>

                    <group string="Rifornimento, Pulizia, Offerta">
                        <field name="rifornimento" readonly="state != 'draft'"/>
                        <field name="rifornimento_euro" readonly="state != 'draft'"/>
                        <field name="rifornimento_litri" readonly="state != 'draft'"/>
                        <field name="pulizia" readonly="state != 'draft'"/>
                        <field name="offerta" readonly="state != 'draft'"/>
                        <field name="offerta_euro" readonly="state != 'draft'"/>
                        <field name="offerta_ricevuta" readonly="state != 'draft'"/>
                    </group>

                    <notebook>
                        <page string="Squadra" name="squadra">
                            <field name="squadra_ids" readonly="state != 'draft'">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="ruolo"/>
                                    <field name="employee_id"/>
                                </list>
                            </field>
                            <field name="numero_volontari" readonly="1"/>
                        </page>

                        <page string="Modalità e Annotazioni" name="annotazioni">
                            <group>
                                <field name="note" nolabel="1"
                                       placeholder="Modalità di svolgimento e annotazioni..."
                                       readonly="state != 'draft'"/>
                            </group>
                        </page>

                        <page string="Paziente" name="paziente">
                            <field name="paziente_ids" readonly="state != 'draft'">
                                <list editable="bottom">
                                    <field name="cognome_nome"/>
                                    <field name="nato_a"/>
                                    <field name="nato_il"/>
                                    <field name="residente_a"/>
                                    <field name="indirizzo"/>
                                    <field name="professione"/>
                                    <field name="sospetta_diagnosi"/>
                                    <field name="causato_da"/>
                                </list>
                            </field>
                        </page>

                        <page string="Consensi" name="consensi">
                            <field name="consenso_ids" readonly="state != 'draft'">
                                <list editable="bottom">
                                    <field name="consenso_fornito"/>
                                    <field name="rifiuto_ricovero"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- ═══════════ LIST VIEW ═══════════ -->
    <record id="view_volontariato_intervento_list" model="ir.ui.view">
        <field name="name">volontariato.intervento.list</field>
        <field name="model">volontariato.intervento</field>
        <field name="arch" type="xml">
            <list decoration-muted="state == 'cancelled'" decoration-success="state == 'confirmed'">
                <field name="codice"/>
                <field name="data"/>
                <field name="vehicle_id"/>
                <field name="tipo_intervento"/>
                <field name="richiedente"/>
                <field name="luogo"/>
                <field name="km_totali"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-success="state == 'confirmed'"
                       decoration-muted="state == 'cancelled'"/>
            </list>
        </field>
    </record>

    <!-- ═══════════ SEARCH VIEW ═══════════ -->
    <record id="view_volontariato_intervento_search" model="ir.ui.view">
        <field name="name">volontariato.intervento.search</field>
        <field name="model">volontariato.intervento</field>
        <field name="arch" type="xml">
            <search>
                <field name="codice"/>
                <field name="vehicle_id"/>
                <field name="richiedente"/>
                <field name="luogo"/>
                <filter string="Bozza" name="state_draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Confermato" name="state_confirmed" domain="[('state', '=', 'confirmed')]"/>
                <filter string="Annullato" name="state_cancelled" domain="[('state', '=', 'cancelled')]"/>
            </search>
        </field>
    </record>

    <!-- ═══════════ ACTION ═══════════ -->
    <record id="action_volontariato_intervento" model="ir.actions.act_window">
        <field name="name">Registro Interventi</field>
        <field name="res_model">volontariato.intervento</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
