<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_volontariato_tipo_intervento_list" model="ir.ui.view">
        <field name="name">volontariato.tipo.intervento.list</field>
        <field name="model">volontariato.tipo.intervento</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="action_volontariato_tipo_intervento" model="ir.actions.act_window">
        <field name="name">Tipi Intervento</field>
        <field name="res_model">volontariato.tipo.intervento</field>
        <field name="view_mode">list</field>
    </record>

</odoo>
